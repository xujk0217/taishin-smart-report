# AI 報表轉簡報自動化系統 — 產品提案

> 通用型 AI 驅動報表分析與簡報生成平台

---

## 一、系統概述

**產品定位**：AI 驅動的「多報表 → 專業簡報」自動化生成系統

使用者上傳任意格式的報表資料（Excel、CSV、PDF），輸入自然語言分析需求後，系統透過多步驟 AI Pipeline 自動完成：

1. 數據智能解析：辨識多表關聯、欄位對應、加總邏輯
2. 指標探索與可行性驗證
3. 數據計算與策略洞察生成
4. PPT 模板綁定與簡報架構設計
5. 原生可編輯 PPTX + 對應 Excel 圖表資料同步輸出
6. 自動寄送至指定收件人

**核心價值**：每一個數字都可追溯、每一張圖表都可獨立編輯、每一份輸出都有完整數值來源。

---

## 二、核心功能模組

### 2.1 數據智能解析引擎

**功能**：自動辨識多張報表間的關聯、欄位對應、加總邏輯

- **多表關聯辨識**：上傳多張報表時，AI 自動比對欄位名稱、資料型態，辨識可合併的維度（如日期欄、品項欄、部門欄）
- **欄位對應**：支援不同報表使用不同欄位名稱但指向相同概念的情況（例如「金額」vs「Amount」）
- **加總邏輯推導**：自動識別加總列/欄（如「合計」、「Total」），建立數據樹狀結構
- **時間序列偵測**：辨識月份、季度、年度等時間欄位，自動排序與合併
- **數據品質檢查**：偵測缺值、異常值、單位不一致等問題

### 2.2 PPT 模板綁定與生成

**功能**：依據預設 PPT 版型，自動填入數據、生成圖表，確保格式一致且數字綁定準確

- **模板上傳**：使用者可上傳自有品牌 .pptx 模板
- **版面辨識**：系統分析模板頁面（封面、內容、圖表頁、封底）
- **數據綁定**：圖表數據直接來自確定性計算引擎，非 AI 生成
- **原生圖表**：輸出圓餅圖、長條圖、折線圖為 PowerPoint 原生圖表物件（可右鍵「編輯資料」）
- **格式一致性**：字型、配色、版面遵守使用者模板規範

### 2.3 Excel 繪圖資料同步輸出

**功能**：生成 PPT 的同時，自動輸出一份與簡報圖表對應的 Excel 檔案

Excel 輸出包含：
- **圖表原始資料**：每張圖表的數據來源與計算過程
- **原生圖表物件**：Excel 內含與 PPT 一致的圖表，可獨立修改
- **來源追溯表**：每個數值的原始檔案、工作表、儲存格位置
- **計算公式表**：所有衍生指標的計算步驟
- **驗證報告**：檢查是否有數字未對上來源的情況

### 2.4 自動寄送機制

**功能**：透過模擬信箱方式，自動將生成的簡報寄送至指定收件人

- **多收件人支援**：填入多位收件人 Email
- **附件管理**：自動附上 PPTX + 對應 XLSX
- **稽核記錄**：每次寄送留下完整紀錄（時間、收件人、內容摘要）
- **模擬模式**：Demo 環境下記錄操作但不實際發送
- **排程寄送**（擴充）：可設定定期產出與寄送

### 2.5 口述驅動模式（加分項）

**功能**：支援語音轉文字，透過 AI Agent 接收口述需求

- **即時語音辨識**：使用 Web Speech API（可擴充 Amazon Transcribe）
- **中文 + 英文混合**：支援中英夾雜的口述指令
- **即時反饋**：語音輸入即時顯示文字
- **跨表指令**：口述「把 A 表的營收和 B 表的成本做比較」等複雜指令
- **財務指標計算**：口述「算出毛利率趨勢」等需求，系統自動拆解為計算步驟

---

## 三、完整流程與階段說明

### 使用者旅程（7 個階段）

```
① 上傳 → ② AI 分析 → ③ 確認計劃 → ④ 執行 → ⑤ 預覽編輯 → ⑥ 輸出 → ⑦ 寄送
```

| 階段 | 使用者動作 | 系統行為 |
|------|-----------|---------|
| ① 上傳 | 拖入報表檔案 + 輸入需求（文字/語音）+ 可選模板 | 多格式解析、自動偵測資料結構與跨表關聯 |
| ② AI 分析 | 等待，觀看進度 | 執行 5 步驟 AI Pipeline（數據解析→指標探索→洞察→架構→驗證） |
| ③ 確認計劃 | 審閱指標、假設、洞察，可修改後核准 | 呈現 AI 規劃結果供人工確認 |
| ④ 執行 | 等待 | 確定性指標計算 + AI 簡報 JSON 生成 + 驗證 |
| ⑤ 預覽編輯 | 瀏覽簡報、自然語言修改、查看數據來源 | 即時 Canvas 渲染 + 護欄式編輯 |
| ⑥ 輸出 | 點擊下載 | 生成 PPTX（原生圖表）+ XLSX（對應圖表資料）|
| ⑦ 寄送 | 填入收件人並寄出 | 模擬/實際寄送 + 稽核紀錄 |

---

## 四、AI Pipeline 流程控制（核心技術）

### 4.1 Pipeline 五步驟架構

```
┌─────────────────────────────────────────────────────────┐
│                    AI Pipeline (5 Steps)                  │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Step 1: Brief（需求解讀）                                │
│    └─ 判斷受眾、目的、語氣、頁數、設計指令                    │
│                                                          │
│  Step 2: Metrics（指標探索）                               │
│    ├─ Pass 1: 從 Prompt 提取使用者要的指標                  │
│    ├─ Pass 2: 逐項判斷「能不能從現有資料計算」               │
│    └─ Pass 3: 補齊遺漏指標                                │
│                                                          │
│  Step 3: Insights（策略洞察）                              │
│    └─ 依受眾深度，為每個面向生成含數據的洞察                  │
│                                                          │
│  Step 4: Blueprint（簡報架構）                             │
│    ├─ Phase A: 段落大綱（頁數分配）                         │
│    └─ Phase B: 逐段展開（每頁標題、訊息、元素）              │
│                                                          │
│  Step 5: Compliance（一致性驗證）                           │
│    └─ 比對原始需求，找出遺漏，自動補齊                       │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 4.2 數據智能解析流程

```
上傳報表
  │
  ├─ 格式辨識（Excel/CSV/PDF）
  │
  ├─ 結構分析
  │   ├─ 欄位名稱與型態辨識
  │   ├─ 時間維度偵測（月/季/年、西元/民國）
  │   ├─ 實體維度偵測（部門/產品/區域/人員）
  │   └─ 數值維度偵測（金額/數量/比率）
  │
  ├─ 多表關聯分析
  │   ├─ 共同維度配對
  │   ├─ 加總列辨識
  │   └─ 時間序列對齊
  │
  └─ 輸出：統一數據模型
      ├─ SourceRef[]（每個數值的來源座標）
      ├─ MetricRecord[]（計算後的指標）
      └─ ChartDataSet[]（可視化資料）
```

### 4.3 各步驟 AI 判斷邏輯

#### Step 1: Brief — 需求解讀

**輸入**：使用者 Prompt + 報表結構摘要

**AI 判斷項目**：
- `audience`：報告給誰看
- `purpose`：報告目的
- `tone`：語氣風格
- `depth`：executive / detailed / technical
- `requestedPageCount`：使用者明確指定的頁數
- `designDirectives`：版面、配色要求
- `narrativeStyle`：文字敘述風格
- `chartPreferences`：偏好圖表類型（圓餅圖、長條圖、折線圖等）
- `constraints`：硬性限制

#### Step 2: Metrics — 指標探索（三輪對話）

**第一輪**：從 Prompt 抽取使用者提到的所有指標名稱

**第二輪**：逐項判斷每個指標是否可從現有欄位計算
- 可計算 → 歸入 `metrics`，附上公式
- 不可計算 → 歸入 `unsupported`，說明缺少什麼資料

**第三輪**：自動偵測遺漏，補齊使用者要求但前兩輪未覆蓋的指標

**通用計算方式**：
| 指標類型 | 公式 |
|---------|------|
| 佔比/市占率 | 個體 ÷ 總計 × 100% |
| 月增率(MoM) | (本期 − 上期) ÷ 上期 × 100% |
| 年增率(YoY) | (本期 − 去年同期) ÷ 去年同期 × 100% |
| 排名 | 依數值大小排序 |
| 佔比 | 項目 ÷ 合計 × 100% |
| 毛利率 | (營收 − 成本) ÷ 營收 × 100% |
| 成長率 | (期末 − 期初) ÷ 期初 × 100% |
| 平均值 | 總和 ÷ 數量 |

#### Step 3: Insights — 策略洞察生成

每個洞察包含：
- `topic`：主題
- `keyFinding`：核心發現（含數字）
- `dataPoints`：2-4 個支撐數據
- `implication`：意義（So What）
- `recommendation`：建議（Now What）
- `chartSuggestion`：建議圖表類型

#### Step 4: Blueprint — 簡報架構設計

- 決定段落數、每段角色與頁數分配
- 逐頁設計：pageTitle、layout、message、elements
- 確保視覺節奏、數據與解讀成對

#### Step 5: Compliance — 一致性驗證

- 使用者每個指標是否都在 metrics 裡
- 每個分析面向是否都有 insight
- 報告對象、語氣是否在架構中體現

---

## 五、AWS 服務架構規劃

### 5.1 架構圖

```
┌─────────────────────────────────────────────────────────────────┐
│                         使用者瀏覽器                              │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AWS Amplify Hosting                            │
│              （React SPA 靜態託管 + CDN）                          │
└───────────────────────────────┬──────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│               Amazon API Gateway (HTTP API)                       │
│                    + Amazon Cognito 認證                          │
└──────┬─────────────┬──────────────────┬──────────────────────────┘
       │             │                  │
       ▼             ▼                  ▼
┌─────────────┐ ┌──────────┐ ┌─────────────────────────┐
│  Lambda     │ │  Lambda  │ │   AWS Step Functions     │
│  API        │ │  Upload  │ │   (Standard Workflow)    │
│  Handlers   │ │  Handler │ │                          │
└─────────────┘ └────┬─────┘ └────────────┬────────────┘
                     │                     │
                     ▼                     ▼
              ┌─────────────┐    ┌─────────────────────┐
              │  Amazon S3  │    │  Workflow 步驟：      │
              │  (Input)    │    │                      │
              └─────────────┘    │  1. Parse Reports    │
                                 │     (Lambda Python)  │
                                 │                      │
                                 │  2. Detect Relations │
                                 │     (Lambda Python)  │
                                 │                      │
                                 │  3. Compute Metrics  │
                                 │     (Lambda Python)  │
                                 │                      │
                                 │  4. AI Pipeline      │
                                 │     (Amazon Bedrock) │
                                 │                      │
                                 │  5. Generate Slides  │
                                 │     (Amazon Bedrock) │
                                 │                      │
                                 │  6. Validate         │
                                 │     (Lambda)         │
                                 │                      │
                                 │  7. Render PPTX+XLSX │
                                 │     (Lambda Node.js) │
                                 │                      │
                                 │  8. Send Email       │
                                 │     (Amazon SES)     │
                                 │                      │
                                 └──────────┬──────────┘
                                            │
                                            ▼
                                 ┌─────────────────────┐
                                 │  Amazon S3           │
                                 │  (Artifacts Output)  │
                                 └─────────────────────┘
```

### 5.2 AWS 服務清單

| 服務 | 用途 |
|------|------|
| **Amazon Bedrock** | LLM 推論 — 5 步驟 AI Pipeline + 簡報生成 + 驗證 |
| **Amazon Bedrock Guardrails** | 內容安全與合規 |
| **AWS Step Functions** | 流程編排、人工審核等待、錯誤處理與重試 |
| **AWS Lambda** | Python: 報表解析、指標計算；Node.js: PPTX/XLSX 渲染 |
| **Amazon S3** | 上傳檔案、計算結果、輸出產物儲存 |
| **Amazon API Gateway** | HTTP API 入口 |
| **Amazon Cognito** | 身份認證與權限控制 |
| **Amazon SES** | 實際郵件寄送（生產環境）|
| **Amazon DynamoDB** | Job 狀態管理 |
| **Amazon CloudWatch** | 可觀測性與監控 |
| **AWS Amplify Hosting** | 前端部署 + CDN |
| **AWS CDK** | 基礎設施即程式碼 |
| **Amazon Transcribe** | 語音轉文字（口述模式）|
| **Kiro** | AI 開發工具 |

---

## 六、設計原則

1. **Evidence first**：任何 AI 洞察前，先建立已驗證且不可變更的計算結果
2. **Deterministic arithmetic**：所有公式、排名、比率與圖表資料由程式計算
3. **通用性優先**：系統不預設任何特定行業或資料格式
4. **模板可替換**：PPT 版型完全由使用者決定
5. **圖表可編輯**：所有圖表為原生物件，非圖片
6. **來源可追溯**：每個數字都能回查原始儲存格
7. **Human in the loop**：計算計劃必須經人工確認
8. **Excel 同步輸出**：簡報中每張圖表都有對應的 Excel 資料與圖表

---

## 七、技術亮點

| 功能 | 說明 |
|------|------|
| 數據智能解析 | AI 自動辨識多表關聯、欄位對應、加總邏輯，無需手動設定 |
| PPT 模板綁定 | 使用者上傳自有模板，系統自動在正確位置填入數據與圖表 |
| Excel 同步輸出 | 每張圖表都有對應的 Excel 工作表，含原始資料與原生圖表物件 |
| 確定性計算 | 所有數字由程式計算而非 AI 生成，確保正確性 |
| 來源追溯 | 每個指標記錄完整計算過程與來源儲存格位置 |
| 護欄式編輯 | 使用者可用自然語言修改簡報，但系統保護數字不被竄改 |
| 語音驅動 | 支援口述需求，AI 自動拆解為計算與簡報步驟 |
| 自動寄送 | 完成即可一鍵寄送至指定收件人 |
