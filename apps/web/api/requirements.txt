python-pptx==1.0.2
# Keep the Lambda bundle on a published manylinux wheel. The latest Pillow
# release available to this SAM image resolves to source-only and requires
# system JPEG headers that are not present in the managed build image.
Pillow==10.4.0
